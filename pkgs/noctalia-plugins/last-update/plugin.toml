# Noctalia plugin: a bar widget showing how stale the auto-update source is.
# See pkgs/scripts/noctalia-last-update.sh for the freshness probe and
# modules/home/desktop/noctalia.nix for how it is installed + wired into the bar.
id = "danielmauderer/last-update"
name = "Auto-update freshness"
min_noctalia = "5.0.0"

[[widget]]
id = "last-update"
entry = "main.luau"
