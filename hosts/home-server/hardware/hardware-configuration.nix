# Placeholder — OVERWRITTEN by nixos-anywhere's --generate-hardware-config at
# install time with the real probed hardware config (see INSTALL.md). Kept
# minimal so the flake still evaluates before that first install run.
{ }
